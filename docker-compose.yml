services:
  haushaltsbuch:
    image: ghcr.io/soloherode/haushaltsbuch:latest
    container_name: haushaltsbuch
    restart: unless-stopped
    ports:
      - "8001:8000"
    volumes:
      - haushaltsbuch_data:/app/data

volumes:
  haushaltsbuch_data:
